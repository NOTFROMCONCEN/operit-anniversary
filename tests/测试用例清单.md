# 测试用例清单

> 对应任务书 [`docs/example.md` 第 667-677 行`](../docs/example.md:667) 的 10 条用例 + 4 条补充用例 + 4 个非法格式边界 + NOT_FOUND。
> 可执行验证脚本：[`../_selftest.js`](../_selftest.js:1)（Node.js 环境，mock 宿主对象）

运行方式：

```bash
node _selftest.js
```

---

## 一、任务书规定用例（10 条）

### 用例 1：添加 2026-07-03 认识纪念日

| 项   | 内容                                                                     |
| ---- | ------------------------------------------------------------------------ |
| 调用 | `add_anniversary({ title: "认识纪念日", date: "2026-07-03" })`           |
| 预期 | `success: true`，`item.title = "认识纪念日"`，`item.date = "2026-07-03"` |
| 验证 | 数据写入文件，`list_anniversaries` 可见                                  |
| 结果 | ✅ PASS                                                                   |

### 用例 2：today = 2026-07-03 时查询

| 项   | 内容                                                                              |
| ---- | --------------------------------------------------------------------------------- |
| 调用 | `get_anniversary_status({ id, today: "2026-07-03" })`                             |
| 预期 | `daysSince = 0`，`daysUntilNext = 0`，`nextDate = "2026-07-03"`，`isToday = true` |
| 结果 | ✅ PASS                                                                            |

### 用例 3：today = 2026-07-04 时查询

| 项   | 内容                                                                                 |
| ---- | ------------------------------------------------------------------------------------ |
| 调用 | `get_anniversary_status({ id, today: "2026-07-04" })`                                |
| 预期 | `daysSince = 1`，`nextDate = "2027-07-03"`，`daysUntilNext = 364`，`isToday = false` |
| 结果 | ✅ PASS                                                                               |

### 用例 4：today = 2026-07-02 时查询

| 项   | 内容                                                     |
| ---- | -------------------------------------------------------- |
| 调用 | `get_anniversary_status({ id, today: "2026-07-02" })`    |
| 预期 | `daysSince = -1`，`daysUntilNext = 1`，`isToday = false` |
| 结果 | ✅ PASS                                                   |

### 用例 5：添加非法日期 2026-02-30

| 项   | 内容                                                     |
| ---- | -------------------------------------------------------- |
| 调用 | `add_anniversary({ title: "非法", date: "2026-02-30" })` |
| 预期 | `success: false`，`error.code = "INVALID_DATE"`          |
| 结果 | ✅ PASS                                                   |

### 用例 6：添加平年 2026-02-29（v2 迭代后）

| 项   | 内容                                                                  |
| ---- | --------------------------------------------------------------------- |
| 调用 | `add_anniversary({ title: "平年闰日", date: "2026-02-29" })`          |
| 预期 | `success: false`，`error.code = "INVALID_DATE"`                       |
| 说明 | v2 迭代后支持闰年 2-29；平年 2-29 为非法日历日期，归入 `INVALID_DATE` |
| 结果 | ✅ PASS                                                                |

### 用例 7：开启 sendToContext 后 get_context 能返回

| 项   | 内容                                                                                                       |
| ---- | ---------------------------------------------------------------------------------------------------------- |
| 调用 | `toggle_anniversary_context({ id, enabled: true })` → `get_context_anniversaries({ today: "2026-07-03" })` |
| 预期 | toggle `success: true`、`sendToContext: true`；context `count = 1`、`items[0].title = "认识纪念日"`        |
| 结果 | ✅ PASS                                                                                                     |

### 用例 8：关闭 sendToContext 后 get_context 不返回

| 项   | 内容                                                                                                        |
| ---- | ----------------------------------------------------------------------------------------------------------- |
| 调用 | `toggle_anniversary_context({ id, enabled: false })` → `get_context_anniversaries({ today: "2026-07-03" })` |
| 预期 | context `count = 0`                                                                                         |
| 结果 | ✅ PASS                                                                                                      |

### 用例 9：删除后 list 默认不返回

| 项   | 内容                                                    |
| ---- | ------------------------------------------------------- |
| 调用 | `delete_anniversary({ id })` → `list_anniversaries({})` |
| 预期 | delete `success: true`；list `count = 0`                |
| 结果 | ✅ PASS                                                  |

### 用例 10：includeDeleted = true 时可见 deleted = true

| 项   | 内容                                           |
| ---- | ---------------------------------------------- |
| 调用 | `list_anniversaries({ includeDeleted: true })` |
| 预期 | `count = 1`，`items[0].deleted = true`         |
| 结果 | ✅ PASS                                         |

---

## 二、补充用例（4 条）

### 用例 11：JSON 损坏时不静默覆盖

| 项       | 内容                                                                                 |
| -------- | ------------------------------------------------------------------------------------ |
| 前置     | 手工将数据文件内容设为 `{not valid json}`                                            |
| 调用     | `list_anniversaries({})`                                                             |
| 预期     | `success: false`，`error.code = "STORAGE_READ_FAILED"`；文件内容保持不变（未被覆盖） |
| 对应验收 | JSON 损坏时不会静默覆盖数据（[`docs/example.md` 第 692 行`](../docs/example.md:692)）             |
| 结果     | ✅ PASS                                                                               |

### 用例 12：重复 title 返回 MULTIPLE_MATCHES

| 项       | 内容                                                                            |
| -------- | ------------------------------------------------------------------------------- |
| 前置     | 添加两条同名不同日期的纪念日                                                    |
| 调用     | `get_anniversary_status({ title: "同名", today: "2026-07-03" })`                |
| 预期     | `success: false`，`error.code = "MULTIPLE_MATCHES"`，`error.matches.length = 2` |
| 对应规则 | [`docs/example.md` 第 441 行`](../docs/example.md:441)                                       |
| 结果     | ✅ PASS                                                                          |

### 用例 13：id/title 均未传返回 INVALID_ARGUMENT

| 项       | 内容                                                |
| -------- | --------------------------------------------------- |
| 调用     | `get_anniversary_status({ today: "2026-07-03" })`   |
| 预期     | `success: false`，`error.code = "INVALID_ARGUMENT"` |
| 对应规则 | [`docs/example.md` 第 599 行`](../docs/example.md:599)           |
| 结果     | ✅ PASS                                              |

### 用例 14：重启后数据仍在

| 项       | 内容                                                                        |
| -------- | --------------------------------------------------------------------------- |
| 调用     | 添加纪念日后，再次调用 `list_anniversaries({})`（数据从文件读取，模拟重启） |
| 预期     | `count = 1`，`items[0].title` 正确                                          |
| 对应验收 | 重启后数据还在（[`docs/example.md` 第 685 行`](../docs/example.md:685)）                 |
| 结果     | ✅ PASS                                                                      |

---

## 三、非法格式边界（4 条）

| 输入 date                | 预期           | 结果   |
| ------------------------ | -------------- | ------ |
| `2026/07/03`（斜杠格式） | `INVALID_DATE` | ✅ PASS |
| `07-03-2026`（倒序）     | `INVALID_DATE` | ✅ PASS |
| `2026-13-01`（非法月份） | `INVALID_DATE` | ✅ PASS |
| 不存在的 id 查询         | `NOT_FOUND`    | ✅ PASS |

---

## 四、验收标准对照（[`docs/example.md` 第 678-692 行`](../docs/example.md:678)）

| 验收项                                         | 对应用例                               | 状态   |
| ---------------------------------------------- | -------------------------------------- | ------ |
| 能在 Operit 里被识别成工具                     | METADATA + `exports`（官方格式已验证） | ✅      |
| 能正常添加纪念日                               | 用例 1                                 | ✅ PASS |
| 能保存到 JSON                                  | 用例 1 + 存储层                        | ✅ PASS |
| 重启后数据还在                                 | 用例 14                                | ✅ PASS |
| 能查询已过去天数                               | 用例 2/3/4                             | ✅ PASS |
| 能查询距离下一次纪念日天数                     | 用例 2/3/4                             | ✅ PASS |
| 能打开和关闭上下文发送                         | 用例 7/8                               | ✅ PASS |
| get_context_anniversaries 只返回允许发送的项目 | 用例 7/8                               | ✅ PASS |
| 非法日期不会写入文件                           | 用例 5/6 + 边界                        | ✅ PASS |
| 删除是软删除                                   | 用例 9/10                              | ✅ PASS |
| JSON 损坏时不会静默覆盖数据                    | 用例 11                                | ✅ PASS |

---

## 五、测试结果汇总

- **总断言数**：84
- **通过**：84
- **失败**：0
- **覆盖率**：任务书全部 10 条用例 + 全部 11 条验收标准 + v2 闰日周年计算专项 + v3 自动注入控制流专项

自测输出（节选）：

```
PASS: 用例1 添加成功
PASS: 用例2 daysSince=0
PASS: 用例3 daysSince=1 / nextDate=2027-07-03 / daysUntilNext=364
PASS: 用例4 daysSince=-1 / daysUntilNext=1
PASS: 用例5 INVALID_DATE
PASS: 用例6 平年2-29 INVALID_DATE
PASS: 用例7 context数量=1
PASS: 用例8 context数量=0
PASS: 用例9 list数量=0
PASS: 用例10 deleted=true
PASS: 用例11 STORAGE_READ_FAILED / 文件未被覆盖
PASS: 用例12 MULTIPLE_MATCHES / matches=2
PASS: 用例13 INVALID_ARGUMENT
PASS: 用例14 持久化
PASS: 边界 2026/07/03 / 07-03-2026 / 2026-13-01 拒绝
PASS: NOT_FOUND
PASS: 闰日添加成功 / 闰日日期正确
PASS: 闰日 当天 isToday=true / daysSince=0 / nextDate=2024-02-29
PASS: 平年闰日 nextDate回退2025-02-28 / isToday=true / daysSince=365
PASS: 平年3月 nextDate=2026-02-28(次年回退)
PASS: 闰年2028当天 isToday=true / nextDate=2028-02-29
PASS: 闰日 context数量=1 / context isToday(平年回退日)
PASS: 注入A isToday=true / daysSince=0 / daysUntilNext=0
PASS: 注入B daysSince=1 / daysUntilNext=364
PASS: 注入C count_up daysSince=731
PASS: 注入D count_down daysUntilNext=5
PASS: 钩子1 非目标阶段返回null
PASS: 钩子2 无开启项返回null
PASS: 钩子3 有开启项返回非null / 含注入标记 / 含纪念日名称
PASS: 钩子4 event字段兼容返回非null
PASS: 钩子5 数据损坏静默返回null
=== 自测完成 ===
```

---

## 六、v2 迭代：闰日（2-29）纪念日支持

| 用例              | 调用                                                   | 预期                                                     | 结果   |
| ----------------- | ------------------------------------------------------ | -------------------------------------------------------- | ------ |
| 闰年 2-29 添加    | `add_anniversary({ title:"闰日", date:"2024-02-29" })` | `success:true`，`date="2024-02-29"`                      | ✅ PASS |
| 平年 2-29 拒绝    | `add_anniversary({ title:"平年", date:"2026-02-29" })` | `INVALID_DATE`                                           | ✅ PASS |
| 闰年当天查询      | `get_anniversary_status({ id, today:"2024-02-29" })`   | `isToday=true`，`nextDate=2024-02-29`，`daysSince=0`     | ✅ PASS |
| 平年回退日查询    | `get_anniversary_status({ id, today:"2025-02-28" })`   | `nextDate=2025-02-28`，`isToday=true`，`daysSince=365`   | ✅ PASS |
| 平年已过回退日    | `get_anniversary_status({ id, today:"2025-03-01" })`   | `nextDate=2026-02-28`（次年仍平年回退），`isToday=false` | ✅ PASS |
| 下一闰年当天      | `get_anniversary_status({ id, today:"2028-02-29" })`   | `isToday=true`，`nextDate=2028-02-29`                    | ✅ PASS |
| 闰日 context 注入 | `get_context_anniversaries({ today:"2025-02-28" })`    | `count=1`，`isToday=true`（平年回退日）                  | ✅ PASS |

**设计说明**：闰日纪念日在平年回退到 2 月 28 日庆祝（业界通行做法），逐年回退到 2-28，直到遇到闰年才在 2-29 庆祝。

---

## 七、v3 迭代：对话自动注入（systemPromptHook）

新增 `systemPromptHook` 钩子，在 `after_compose_system_prompt` 阶段把发送开关已开启的纪念日状态自动注入系统提示词。测试分两组：摘要文案验证（经 `get_context_anniversaries` 间接验证 `formatContextSummary` 的 mode 分支）与钩子控制流验证。

### 摘要文案（formatContextSummary 按 mode 分支）

| 用例             | 调用                                                                 | 预期                                                   | 结果   |
| ---------------- | -------------------------------------------------------------------- | ------------------------------------------------------ | ------ |
| 注入A 当天 both  | `get_context_anniversaries({ today:"2026-07-03" })`，base=2026-07-03 | `isToday=true`，`daysSince=0`，`daysUntilNext=0`       | ✅ PASS |
| 注入B 次日 both  | `get_context_anniversaries({ today:"2026-07-04" })`                  | `daysSince=1`，`daysUntilNext=364`                     | ✅ PASS |
| 注入C count_up   | base=2024-01-01，today=2026-01-01                                    | `isToday=true`，`daysSince=731`（2024闰366+2025平365） | ✅ PASS |
| 注入D count_down | base=2026-12-25，today=2026-12-20                                    | `daysUntilNext=5`，`isToday=false`                     | ✅ PASS |

### 钩子控制流（systemPromptHook）

| 用例             | 调用                                                                    | 预期                                          | 结果   |
| ---------------- | ----------------------------------------------------------------------- | --------------------------------------------- | ------ |
| 钩子1 非目标阶段 | `systemPromptHook({ eventName:"some_other_stage" })`                    | 返回 `null`（不修改）                         | ✅ PASS |
| 钩子2 无开启项   | `systemPromptHook({ eventName:"after_compose_system_prompt" })`，无数据 | 返回 `null`                                   | ✅ PASS |
| 钩子3 有开启项   | 同上，已加 `sendToContext:true` 纪念日                                  | 返回 `{systemPrompt}`，含「纪念日提醒」+ 名称 | ✅ PASS |
| 钩子4 event字段  | `systemPromptHook({ event:"after_compose_system_prompt" })`             | 兼容 `event` 字段，返回非 null                | ✅ PASS |
| 钩子5 数据损坏   | 数据文件为 `{broken json`                                               | 静默返回 `null`，不阻断对话                   | ✅ PASS |

**设计说明**：`systemPromptHook` 只在 `after_compose_system_prompt` 阶段工作；筛选 `deleted=false && sendToContext=true` 的记录，按 `mode` 生成自然语言摘要拼入系统提示词；任何异常都静默返回 `null`，绝不阻断用户对话。`get_context_anniversaries` 工具仍保留，供 AI 需要结构化数据时主动调用。

---

## 八、v4 迭代：原生 UI 增强版（native_ui）

原生 UI 增强版采用多文件包结构 + `compose_dsl` 声明式 UI，核心逻辑从基础版抽取为独立 service 层。测试脚本：[`../native_ui/_selftest.js`](../native_ui/_selftest.js:1)，共 59 项断言。

运行方式：

```bash
node native_ui/_selftest.js
```

### 日期计算（service 层，与基础版一致）

| 用例           | 调用                                              | 预期                                 | 结果   |
| -------------- | ------------------------------------------------- | ------------------------------------ | ------ |
| 闰年 2024      | `isLeapYear(2024)`                                | `true`                               | ✅ PASS |
| 平年 2023      | `isLeapYear(2023)`                                | `false`                              | ✅ PASS |
| 世纪闰年 2000  | `isLeapYear(2000)`                                | `true`                               | ✅ PASS |
| 世纪平年 1900  | `isLeapYear(1900)`                                | `false`                              | ✅ PASS |
| 闰日有效       | `isValidYMD("2024-02-29")`                        | `{valid:true}`                       | ✅ PASS |
| 平年 2-29 无效 | `isValidYMD("2023-02-29")`                        | `{valid:false}`                      | ✅ PASS |
| 当天状态       | `getAnniversaryStatus("2026-07-03","2026-07-03")` | `daysSince=0, isToday=true`          | ✅ PASS |
| 1天后状态      | `getAnniversaryStatus("2026-07-03","2026-07-04")` | `daysSince=1, daysUntilNext=364`     | ✅ PASS |
| 1天前状态      | `getAnniversaryStatus("2026-07-03","2026-07-02")` | `daysSince=-1, daysUntilNext=1`      | ✅ PASS |
| 闰日平年回退   | `getAnniversaryStatus("2024-02-29","2025-03-01")` | `daysSince=366, nextDate=2026-02-28` | ✅ PASS |
| 闰日当天       | `getAnniversaryStatus("2024-02-29","2024-02-29")` | `isToday=true`                       | ✅ PASS |

### CRUD 全流程（service 层）

| 用例       | 调用                                          | 预期                             | 结果   |
| ---------- | --------------------------------------------- | -------------------------------- | ------ |
| 创建       | `createAnniversary({title,date,owner,mode})`  | 返回含 id 的完整 item            | ✅ PASS |
| 列表       | `listAnniversaries({includeDeleted:false})`   | `length=1`                       | ✅ PASS |
| 获取单个   | `getAnniversary(id)`                          | `title` 正确                     | ✅ PASS |
| 更新       | `updateAnniversary({id,title,sendToContext})` | 字段已更新                       | ✅ PASS |
| 切换上下文 | `toggleContext(id, false)`                    | `sendToContext=false`            | ✅ PASS |
| 删除（软） | `deleteAnniversary(id)`                       | 返回 id，列表为空，含已删除有1条 | ✅ PASS |

### 工具子包（anniversary_tools.js）

| 用例               | 调用                                           | 预期                              | 结果   |
| ------------------ | ---------------------------------------------- | --------------------------------- | ------ |
| add_anniversary    | `tools.add_anniversary({title,date,ctx})`      | `success:true`, `item.title` 正确 | ✅ PASS |
| list_anniversaries | `tools.list_anniversaries({})`                 | `count=1`                         | ✅ PASS |
| get_status 当天    | `tools.get_anniversary_status({id,today})`     | `daysSince=0, isToday=true`       | ✅ PASS |
| get_status 1年后   | `tools.get_anniversary_status({id,today+1y})`  | `daysSince=365, isToday=true`     | ✅ PASS |
| toggle 关闭        | `tools.toggle_anniversary_context({id,false})` | `sendToContext=false`             | ✅ PASS |
| get_context 关闭   | `tools.get_context_anniversaries({today})`     | `count=0`                         | ✅ PASS |
| toggle 开启        | `tools.toggle_anniversary_context({id,true})`  | `sendToContext=true`              | ✅ PASS |
| get_context 开启   | `tools.get_context_anniversaries({today})`     | `count=1, items[0].isToday=true`  | ✅ PASS |
| delete             | `tools.delete_anniversary({id})`               | `success:true`, list 后 `count=0` | ✅ PASS |

### formatContextSummary（三种模式）

| 用例       | mode         | 状态                               | 预期             | 结果   |
| ---------- | ------------ | ---------------------------------- | ---------------- | ------ |
| both 当天  | `both`       | `isToday=true`                     | 1条：「今天是…」 | ✅ PASS |
| both 已过  | `both`       | `daysSince=100, daysUntilNext=265` | 2条：已过+倒计   | ✅ PASS |
| count_up   | `count_up`   | `daysSince=100, daysUntilNext=265` | 1条：仅已过      | ✅ PASS |
| count_down | `count_down` | `daysSince=100, daysUntilNext=265` | 1条：仅倒计      | ✅ PASS |

### systemPromptHook 控制流

| 用例           | 调用                                                          | 预期                                   | 结果   |
| -------------- | ------------------------------------------------------------- | -------------------------------------- | ------ |
| 非目标阶段     | `systemPromptHook({eventName:"other_stage"})`                 | `null`                                 | ✅ PASS |
| 无开启项       | `systemPromptHook({eventName:"after_compose_system_prompt"})` | `null`                                 | ✅ PASS |
| 有开启项       | 同上 + 已创建 `sendToContext:true`                            | 非 null，含原始提示词 + 「纪念日提醒」 | ✅ PASS |
| event 字段兼容 | `systemPromptHook({event:"after_compose_system_prompt"})`     | 非 null                                | ✅ PASS |

### 数据兼容性（与基础版共享存储格式）

| 用例       | 验证内容                                 | 预期                 | 结果   |
| ---------- | ---------------------------------------- | -------------------- | ------ |
| 存储格式   | 读取 JSON 文件                           | `Array.isArray=true` | ✅ PASS |
| 字段完整性 | title/date/sendToContext/calendarType 等 | 全部字段存在且值正确 | ✅ PASS |
| 闰日存储   | `date="2024-02-29"`                      | 原样存储             | ✅ PASS |

**设计说明**：原生 UI 增强版的 service 层逻辑从单文件基础版完整抽取，确保两个版本行为一致、数据兼容。存储层使用相同的 `ToolPkg.getConfigDir()/anniversaries.json` 路径，两个版本可以交替使用同一份数据。UI 页面（compose_dsl）调用 service 层的 CRUD 函数，不直接操作文件。

---

## 九、防剽窃防护测试（v5 新增）

### 基础版（AnniversaryPlugin.js）— 18 项断言

| 用例             | 调用 / 操作                                 | 预期                                      | 结果   |
| ---------------- | ------------------------------------------- | ----------------------------------------- | ------ |
| 完整性校验通过   | `verifyIntegrity(null)`                     | `ok=true`                                 | ✅ PASS |
| 篡改 author      | 修改 `_W.author` 后 `verifyIntegrity(null)` | `ok=false, reason=WATERMARK_TAMPERED`     | ✅ PASS |
| 篡改 salt        | 修改 `_W.salt` 后 `verifyIntegrity(null)`   | `ok=false`                                | ✅ PASS |
| 哨兵完整         | 提供 3 个哨兵映射                           | `ok=true`                                 | ✅ PASS |
| 哨兵缺失         | 缺少 1 个哨兵                               | `ok=false, reason=SENTINEL_MISSING`       | ✅ PASS |
| 数据指纹盖章     | `add_anniversary` 后检查磁盘记录            | 记录含 `_afp` 数值字段                    | ✅ PASS |
| 工具降级（add）  | 篡改 `_W.author` 后调用 `add_anniversary`   | `success=false, code=INTEGRITY_VIOLATION` | ✅ PASS |
| 工具降级（list） | 篡改 `_W.salt` 后调用 `list_anniversaries`  | `success=false, code=INTEGRITY_VIOLATION` | ✅ PASS |
| 恢复后正常       | 恢复 `_W` 后调用 `list_anniversaries`       | `success=true`                            | ✅ PASS |
| 钩子静默降级     | 篡改后调用 `systemPromptHook`               | `null`（不阻断对话）                      | ✅ PASS |

### 原生 UI 版（native_ui）— 21 项断言

| 用例             | 调用 / 操作                                       | 预期                                      | 结果   |
| ---------------- | ------------------------------------------------- | ----------------------------------------- | ------ |
| 完整性校验通过   | `guard.verifyIntegrity(null)`                     | `ok=true`                                 | ✅ PASS |
| 篡改 author      | 修改 `guard._W.author` 后校验                     | `ok=false, reason=WATERMARK_TAMPERED`     | ✅ PASS |
| 篡改 salt        | 修改 `guard._W.salt` 后校验                       | `ok=false`                                | ✅ PASS |
| 创建记录含指纹   | `createAnniversary` 后检查返回对象                | `_afp` 存在且为数值                       | ✅ PASS |
| 磁盘记录含指纹   | 读取 JSON 文件验证                                | `parsed[0]._afp` 存在                     | ✅ PASS |
| 指纹校验有效     | `verifyDataFingerprint(原始记录)`                 | `true`                                    | ✅ PASS |
| 篡改指纹校验失败 | `verifyDataFingerprint(篡改_afp)`                 | `false`                                   | ✅ PASS |
| 无指纹记录失败   | `verifyDataFingerprint(无_afp记录)`               | `false`                                   | ✅ PASS |
| 有效指纹计数     | `countValidFingerprints(记录数组)`                | `1`                                       | ✅ PASS |
| 工具降级（add）  | 篡改后调用 `tools.add_anniversary`                | `success=false, code=INTEGRITY_VIOLATION` | ✅ PASS |
| 工具降级（list） | 篡改后调用 `tools.list_anniversaries`             | `success=false, code=INTEGRITY_VIOLATION` | ✅ PASS |
| 恢复后正常       | 恢复 `guard._W` 后调用 `tools.list_anniversaries` | `success=true`                            | ✅ PASS |
| 钩子静默降级     | 篡改后调用 `service.systemPromptHook`             | `null`                                    | ✅ PASS |
| 哨兵完整         | 提供 3 个哨兵映射                                 | `ok=true`                                 | ✅ PASS |
| 哨兵缺失         | 缺少 1 个哨兵                                     | `ok=false, reason=SENTINEL_MISSING`       | ✅ PASS |

**设计说明**：防剽窃防护采用强力分层策略。功能性水印常量（`_W`）参与数据指纹计算，并非装饰性注释；自校验值（`_SELF_CHECK`）在运行时重算比对。篡改任意水印字段即触发功能降级——工具返回 `INTEGRITY_VIOLATION` 版权警告。数据指纹（`_afp`）写入每条纪念日记录，即使侵权者删除所有可见版权头，磁盘上的记录仍携带原始作者指纹作为取证证据。
